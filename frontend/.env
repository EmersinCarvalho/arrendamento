# Gerido por .env.development e .env.production (Vite seleciona automaticamente)
# LOCAL  → VITE_API_URL=http://localhost:5000      (usado com: npm run dev)
# PRODUÇÃO → VITE_API_URL=https://api.arrendahouse.eu (usado com: npm run build)
